import { Dispatch } from 'react';

const updateStoreProfress = (userData: string, dificulty: any, exercice: string) => ({
  type: 'Updatadawiodw',
  userData,
  exercice,
});

export const updateuserProgress = (): any => {
  return async (dispatch: Dispatch<any>, { userData, exerciceProgress }: any) => {

  };
};
